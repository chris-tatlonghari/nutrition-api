<h1> Services </h1>

<div class="my-3">
    <ul>
        <% @services.each do |service| %>
            <li>
                <h3> <%= link_to(service.name, service_path(service.id)) %> </h3>
            </li>
        <% end %>
    </ul>

</div>
